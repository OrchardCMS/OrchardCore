@model BackgroundTaskViewModel

<h1>@RenderTitleSegments(T["Edit Task Options"])</h1>

<div asp-validation-summary="ModelOnly"></div>
<form asp-action="Edit" method="post">

    <input type="hidden" value="@Model.Name" name="sourceName" />

    <div class="form-group" asp-validation-class-for="Name">
        <label asp-for="Name">@T["Name"]</label>
        <select asp-for="Name" class="form-control">
            @foreach (var name in Model.Names)
            {
                <option value="@name">@name</option>
            }
        </select>
        <div class="hint">@T["The task on which these options will apply."]</div>
    </div>

    <div class="form-group" asp-validation-class-for="Enable">
        <label asp-for="Enable">
            <input asp-for="Enable" type="checkbox" />
            @T["Enable"]
        </label>
        <span class="hint">@T["Whether this task is enabled or not."]</span>
    </div>

    <div class="form-group" asp-validation-class-for="Schedule">
        <label asp-for="Schedule">@T["Schedule"]</label>
        <input asp-for="Schedule" class="form-control" autofocus />
        <span class="hint">@T["The task schedule as a cron expression."]</span>
    </div>

    <div class="form-group" asp-validation-class-for="Description">
        <label asp-for="Description">@T["Description"]</label>
        <input asp-for="Description" class="form-control" />
        <span class="hint">@T["The description of this task."]</span>
    </div>

    <fieldset>
        <div class="form-group">
            <button class="btn btn-primary" type="submit">@T["Save"]</button>
            <a class="btn btn-secondary" asp-route-action="Index">@T["Cancel"]</a>
        </div>
    </fieldset>
</form>
