@model LocalizationPartViewModel

@if (Model.LocalizationPart.Culture != null)
{
    <span class="badge badge-light font-weight-normal" data-toggle="tooltip" title="@T["Culture"]"><i class="fa fa-globe text-info"></i> @Model.LocalizationPart.Culture</span>
}
