@using OrchardCore.Mvc.Utilities

@if (Model.ContentItem.Content.LocalizationPart.Culture != null)
{
    <span class="badge badge-info"><i class="fa fa-globe"></i> @Model.ContentItem.Content.LocalizationPart.Culture</span>
}