@using OrchardCore.ContentManagement

@{
    ContentItem contentItem = Model.ContentItem;
}

<div class="row">
    <div class="col-xl-2 col-sm-6 title">
        <div class="custom-control custom-checkbox float-left">
            <input type="checkbox" class="custom-control-input" value="@contentItem.Id" name="itemIds" id="itemIds-@contentItem.Id">
            <label class="custom-control-label" for="itemIds-@contentItem.Id">&nbsp;</label>
        </div>
        <a admin-for="@contentItem" asp-route-returnUrl="@FullRequestPath" />
    </div>
    <div class="col-xl-1 col-sm-6 type mx-3">
        <small class="text-muted">@T["Widget"]</small>
    </div>

    @if (Model.Header != null)
    {
        <div class="col-1 col-sm-6 header">@await DisplayAsync(Model.Header)</div>
    }

    @if (Model.Meta != null)
    {
        <div class="col-xl-3 col-sm-6 metadata">
            @await DisplayAsync(Model.Meta)
        </div>
    }

    @if (Model.Tags != null)
    {
        <div class="col-xl-2 col-sm-6 tags float-right">
            @await DisplayAsync(Model.Tags)
        </div>
    }

    @if (Model.Actions != null)
    {
        <div class="col-xl-3 col-sm-6 related float-right">
            @await DisplayAsync(Model.Actions)
        </div>
    }
</div>

@if (Model.Content != null)
{
    <div class="primary">@await DisplayAsync(Model.Content)</div>
}
