@Model.Metadata.ChildContent

@* Inject the modal window only once per page *@
@if (!Context.Items.ContainsKey("Media_Modal__MarkdownBody"))
{
    Context.Items["Media_Modal__MarkdownBody"] = new object();
    @await DisplayAsync(await New.Media_Modal__MarkdownBody())
}