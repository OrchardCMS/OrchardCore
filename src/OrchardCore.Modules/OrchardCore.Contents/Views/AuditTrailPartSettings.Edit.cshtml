@model AuditTrailPartSettingsViewModel
@using OrchardCore.Contents.AuditTrail.ViewModels

<div class="form-group">
    <div class="custom-control custom-checkbox">
        <input type="checkbox" asp-for="ShowCommentInput" class="custom-control-input" checked="@Model.ShowCommentInput">
        <label class="custom-control-label" asp-for="ShowCommentInput">@T["Show comment input"]</label>
        <span class="hint">— @T["Wether the audit trail comment input is shown while editing."]</span>
    </div>
</div>
