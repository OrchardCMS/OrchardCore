/**
 * File processing modal is not needed for OrchardCore.
 * This is a stub to maintain the import structure.
 */
export const useFileProcessModal = () => {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const showFileProcessModal = (_modalFileEvent: any) => {
    // No-op in OrchardCore
  };

  return { showFileProcessModal };
};
