@model dynamic

<h1>@RenderTitleSegments(T["Edit User"])</h1>

<form asp-action="Edit" method="post" asp-route-returnUrl="@ViewData["returnUrl"]" enctype="multipart/form-data">
    @Html.ValidationSummary()
    @await DisplayAsync(Model)
</form>
