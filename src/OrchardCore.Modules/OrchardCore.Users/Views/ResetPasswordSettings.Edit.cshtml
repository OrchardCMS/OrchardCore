@model OrchardCore.Users.Models.ResetPasswordSettings

<fieldset class="form-group" asp-validation-class-for="AllowResetPassword">
    <div class="custom-control custom-checkbox">
        <input type="checkbox" class="custom-control-input" asp-for="AllowResetPassword" />
        <label class="custom-control-label" asp-for="AllowResetPassword">@T["Allow the users to reset their password"] <span asp-validation-for="AllowResetPassword"></span></label>
        <span class="hint">— @T["An email will be sent to the user's email address with a reset link."]</span>
    </div>
</fieldset>
