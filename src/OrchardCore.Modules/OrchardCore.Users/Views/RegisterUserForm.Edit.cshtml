<div class="row">
    <div class="col-md-offset-4 col-md-8">
        <h2>@T["Register a new account"]</h2>
        <hr />

        <form asp-action="Register" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post" class="form-horizontal no-multisubmit">

            <div asp-validation-summary="All"></div>

            @if (Model.Content != null)
            {
                @await DisplayAsync(Model.Content)
            }

            <div class="mb-3">
                <button type="submit" class="btn btn-primary">@T["Register"]</button>
            </div>
        </form>
    </div>
</div>
