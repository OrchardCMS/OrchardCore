@model UsersIndexViewModel

<style asp-name="bootstrap-select" at="Head"></style>
<script asp-name="bootstrap-select" at="Foot"></script>
<div class="bulk-select-list" data-checkbox-name="itemIds" data-bulk-action-name="Options.BulkAction" data-selected-text="@T["selected"]">

<!-- Hidden submit button do not remove -->
<input type="submit" name="submit.Filter" id="submitFilter" class="visually-hidden" />
<input type="submit" name="submit.BulkAction" class="visually-hidden" />

@Html.HiddenFor(o => o.Options.BulkAction)

<div class="card text-bg-theme mb-3 position-sticky action-bar">
    <div class="card-body">
        <div class="row gx-2">
            <div class="col">
                @await DisplayAsync(Model.Header.Search)
            </div>
            <div class="col-auto">
                @await DisplayAsync(Model.Header.Create)
            </div>
        </div>
    </div>
</div>

@* The users, the summary/actions bar and the pager are rendered by the AdminList shape using the configured layout (List, Grid, ...). *@
@await DisplayAsync(Model.List)

</div>

<script asp-src="~/OrchardCore.Users/Scripts/users-admin-list/users-admin-list.min.js" debug-src="~/OrchardCore.Users/Scripts/users-admin-list/users-admin-list.js" at="Foot" type="module"></script>
