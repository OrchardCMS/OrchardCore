@{
    TagBuilder optionTag = Tag(Model, "option");
    optionTag.MergeAttributes(Model.Attributes);
    optionTag.Attributes.Add("value", Model.Value);
    optionTag.InnerHtml.Append(Model.Text);
}

@optionTag