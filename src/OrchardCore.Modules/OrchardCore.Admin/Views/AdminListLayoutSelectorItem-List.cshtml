@{
    var isCurrent = Model.IsCurrent != null && (bool)Model.IsCurrent;
}

<a href="@Model.Url" class="btn btn-sm @(isCurrent ? "btn-primary active" : "btn-outline-secondary")" title="@T["List"]" aria-label="@T["List"]" aria-pressed="@isCurrent">
    <i class="fa-solid fa-list" aria-hidden="true"></i>
</a>
