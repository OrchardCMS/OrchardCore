@{
    var isCurrent = Model.IsCurrent != null && (bool)Model.IsCurrent;
}

<a href="@Model.Url" class="btn btn-sm @(isCurrent ? "btn-primary active" : "btn-outline-secondary")" title="@T["Grid"]" aria-label="@T["Grid"]" aria-pressed="@isCurrent">
    <i class="fa-solid fa-table-cells-large" aria-hidden="true"></i>
</a>
