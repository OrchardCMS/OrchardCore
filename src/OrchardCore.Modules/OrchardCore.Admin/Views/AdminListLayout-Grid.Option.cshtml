@{
    string current = Model.ListLayout;
}
<option value="@AdminListConstants.Grid" selected="@string.Equals(current, AdminListConstants.Grid, StringComparison.OrdinalIgnoreCase)">@T["Grid"]</option>
