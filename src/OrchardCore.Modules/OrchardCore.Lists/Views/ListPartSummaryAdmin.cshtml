@using OrchardCore.ContentTypes
@using OrchardCore.Lists.ViewModels
@using OrchardCore.Localization.Data
@model ListPartSummaryAdminViewModel

@inject IDataLocalizer D

@if (Model.ContainedContentTypes.Length == 1)
{
    <a class="btn btn-sm btn-secondary" admin-for="@Model.ContentItem" asp-route-partName="@Model.TypePartDefinition.Name">@T["List {0} Items", D[Model.TypePartDefinition.DisplayName(), DataLocalizationContext.ContentType]]</a>
}
else
{
    <a class="btn btn-sm btn-secondary" admin-for="@Model.ContentItem">@T["List Items"]</a>
}
