@{
    var embedId = Model.Arguments.NamedOrDefault("id") as string;
    if (!String.IsNullOrEmpty(embedId))
    {
        <iframe src="https://giphy.com/embed/@embedId" width="480" height="250" frameBorder="0" class="giphy-embed" allowFullScreen></iframe>
    }
}