@model ContentItem
@using Microsoft.AspNetCore.Html;
@using OrchardCore.ContentManagement;
@using OrchardCore.Demo.Models;

@(Model.As<TestContentPartA>().Line)

@{
    await ThemeLayout.Zones["Footer"].AddAssync("In the footer", "2.0");
    await ThemeLayout.Zones["Footer"].Add("Before", "before");
    await ThemeLayout.Zones["Footer"].Add(new HtmlString("AFTER"), "after");
}
