@model OrchardCore.Media.Azure.ViewModels.OptionsViewModel
<h1>@RenderTitleSegments(T["Azure Blob storage options"])</h1>

<p class="alert alert-secondary">
    @T["Azure Blob storage is configured with appsettings.json."]
    <a class="seedoc" href="@(OrchardCore.Admin.Constants.DocsUrl)reference/modules/Media.Azure/#configuration" target="_blank">@T["See documentation"]</a>
</p>

<div class="@Orchard.GetWrapperClasses()">
    <label asp-for="ConnectionString" class="@Orchard.GetLabelClasses()">@T["Connection String"]</label>
    <div class="@Orchard.GetEndClasses()">
        <code>
            @if (!string.IsNullOrEmpty(Model.ConnectionString))
            {
                @T["Configured"]
            }
            else
            {
                @T["Not Configured"]
            }
        </code>
    </div>
</div>

<div class="@Orchard.GetWrapperClasses()">
    <label asp-for="ContainerName" class="@Orchard.GetLabelClasses()">@T["Container Name"]</label>
    <div class="@Orchard.GetEndClasses()">
        <code><input type="text" class="form-control-plaintext" value="@Model.ContainerName" readonly /></code>
    </div>
</div>

<div class="@Orchard.GetWrapperClasses()">
    <label asp-for="BasePath" class="@Orchard.GetLabelClasses()">@T["Base Path"]</label>
    <div class="@Orchard.GetEndClasses()">
        <code><input type="text" class="form-control-plaintext" value="@Model.BasePath" readonly /></code>
    </div>
</div>

<div class="@Orchard.GetWrapperClasses()">
    <div class="@Orchard.GetEndClasses(true)">
        <div class="form-check">
            <input type="checkbox" class="form-check-input" asp-for="CreateContainer" checked="@Model.CreateContainer" disabled />
            <label class="form-check-label" asp-for="CreateContainer">@T["Create Container"]</label>
            <span class="hint">@T["Whether a blob container is created on startup if it does not exist."]</span>
        </div>
    </div>
</div>

<div class="@Orchard.GetWrapperClasses()">
    <div class="@Orchard.GetEndClasses(true)">
        <div class="form-check">
            <input type="checkbox" class="form-check-input" asp-for="RemoveContainer" checked="@Model.RemoveContainer" disabled />
            <label class="form-check-label" asp-for="RemoveContainer">@T["Remove Container"]</label>
            <span class="hint">@T["Whether a blob container is removed on tenant deletion if it exists."]</span>
        </div>
    </div>
</div>

<div class="@Orchard.GetWrapperClasses()">
    <div class="@Orchard.GetEndClasses(true)">
        <div class="form-check">
            <input type="checkbox" class="form-check-input" asp-for="RemoveFilesFromBasePath" checked="@Model.RemoveFilesFromBasePath" disabled />
            <label class="form-check-label" asp-for="RemoveFilesFromBasePath">@T["Remove Files From Base Path"]</label>
            <span class="hint">@T["Whether only the media files under the base path are removed on tenant deletion."]</span>
        </div>
    </div>
</div>
