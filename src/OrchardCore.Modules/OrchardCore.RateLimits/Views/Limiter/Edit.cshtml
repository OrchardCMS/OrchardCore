@model OrchardCore.RateLimits.ViewModels.RateLimiterEditorViewModel

<zone Name="Title">
    <breadcrumb name="@RateLimitsBreadcrumbs.LimiterEdit" data="@(new { Model.DisplayName })" />
</zone>

<form asp-action="Edit" method="post" class="no-multisubmit">
    <div class="alert alert-info" role="alert">
        <span>@Model.Description</span>
        <a class="seedoc" href="@Model.DocumentationUrl" target="_blank">@T["Learn more"]</a>
    </div>

    @await DisplayAsync(Model.Editor)

    <div class="mt-4 d-flex gap-1">
        <button class="btn btn-primary" type="submit">@T["Save"]</button>
        <a asp-action="Edit" asp-controller="Admin" asp-route-policyId="@ViewContext.RouteData.Values["policyId"]" class="btn btn-secondary">@T["Back"]</a>
    </div>
</form>
