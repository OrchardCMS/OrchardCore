@using OrchardCore
@using OrchardCore.Forms.ViewModels
@model ButtonPartEditViewModel

<div class="ocat-wrapper">
    <label asp-for="Text" class="ocat-label">@T["Text"]</label>
    <div class="ocat-end">
        <input asp-for="Text" type="text" class="form-control content-preview-text" />
        <span class="hint">@T["The text to display on the button."]</span>
    </div>
</div>

<div class="ocat-wrapper">
    <label asp-for="Type" class="ocat-label">@T["Type"]</label>
    <div class="ocat-end">
        <select asp-for="Type" class="form-select content-preview-select">
            <option value="submit">@T["Submit"]</option>
            <option value="button">@T["Button"]</option>
            <option value="reset">@T["Reset"]</option>
        </select>
        <span class="hint">@T["The button type."]</span>
    </div>
</div>
