@*<div class="@Orchard.GetWrapperClasses()">
        <div class="@Orchard.GetEndClasses(true)">
            <span class="hint dashed">@T["This is a settings editor that appears on all type parts"]</span>
        </div>
    </div>*@
