@using OrchardCore.ContentTypes.Models
@using OrchardCore.DisplayManagement.Views

@model ShapeViewModel<ContentPartEntry>

@if (!string.IsNullOrWhiteSpace(Model.Value.Description))
{
    <div class="admin-list-secondary list-item-search-text">@Model.Value.Description</div>
}
