@Model.Metadata.ChildContent

@* Inject the modal window only once per page *@
@if (!Context.Items.ContainsKey("Media_MoreModal__HtmlField"))
{
    Context.Items["Media_MoreModal__HtmlField"] = new object();
    @await DisplayAsync(await New.Media_MoreModal__HtmlField())
}