@Model.Metadata.ChildContent

@* Inject the modal window only once per page *@
@if (!Context.Items.ContainsKey("Media_TinyMceModal__HtmlField"))
{
    Context.Items["Media_TinyMceModal__HtmlField"] = new object();
    @await DisplayAsync(await New.Media_TinyMceModal__HtmlField())
}