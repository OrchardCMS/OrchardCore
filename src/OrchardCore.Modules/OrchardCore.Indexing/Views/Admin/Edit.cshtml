@using OrchardCore.Infrastructure.Entities

@model ModelViewModel

<zone Name="Title">
    <breadcrumb name="IndexesEdit" title="@T["Edit '{0}' index", Model.DisplayName]">
        <breadcrumb-item id="Indexes" action="Index" controller="Admin" area="OrchardCore.Indexing" permission="@IndexingPermissions.ManageIndexes.Name">@T["Indexes"]</breadcrumb-item>
    </breadcrumb>
</zone>

<form method="post" asp-route-id="@ViewContext.HttpContext.Request.RouteValues["Id"]" enctype="multipart/form-data" class="no-multisubmit">
    @Html.ValidationSummary()
    @await DisplayAsync(Model.Editor)

    <button class="btn btn-primary" type="submit">@T["Save"]</button>

    <a class="btn btn-secondary cancel" role="button" asp-route-action="Index">@T["Cancel"]</a>
</form>
