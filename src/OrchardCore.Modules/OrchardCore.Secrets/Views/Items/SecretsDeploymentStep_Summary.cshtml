@model OrchardCore.Secrets.Deployment.SecretsDeploymentStep

<p class="m-0">
    @T["Adds all secrets to the deployment plan."]
    @if (!string.IsNullOrEmpty(Model.EncryptionKeyName))
    {
        <br />
        <span class="badge text-bg-success">@T["Encrypted"]</span>
        <small class="text-muted">@T["using key: {0}", Model.EncryptionKeyName]</small>
    }
    else
    {
        <br />
        <span class="badge text-bg-secondary">@T["Metadata only"]</span>
    }
</p>
