@{
    Model.Metadata.Alternates.Clear();
    Model.Metadata.Type = "Pager_Link";

    if (Model.Tag is TagBuilder parentTag)
    {
        parentTag.AddCssClass("active");
        parentTag.MergeAttribute("aria-current", "page");
    }
}

@await DisplayAsync(Model)
