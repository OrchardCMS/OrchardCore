{% if Model.RightText %}
<li class="timeline-inverted">
	<div class="timeline-image">
		<img class="rounded-circle img-fluid" src="{{Model.ContentItem.Content.Step.Image.Paths.first | asset_url }}" alt="{{Model.ContentItem.Content.TitlePart.Title}}">
	</div>
	<div class="timeline-panel">
		<div class="timeline-heading">
			<h4>{{Model.ContentItem.Content.Step.Date.Text}}</h4>
			<h4 class="subheading">{{ Model.ContentItem.Content.TitlePart.Title}}</h4>
		</div>
		<div class="timeline-body">
			<p class="mth-text-base">{{ Model.ContentItem.Content.Step.Description.Text}}</p>
		</div>
	</div>
</li>
{% else %}
<li>
	<div class="timeline-image">
		<img class="rounded-circle img-fluid" src="{{Model.ContentItem.Content.Step.Image.Paths.first | asset_url }}" alt="{{Model.ContentItem.Content.TitlePart.Title}}">
	</div>
	<div class="timeline-panel">
		<div class="timeline-heading">
			<h4>{{Model.ContentItem.Content.Step.Date.Text}}</h4>
			<h4 class="subheading">{{ Model.ContentItem.Content.TitlePart.Title}}</h4>
		</div>
		<div class="timeline-body">
			<p class="mth-text-base">{{ Model.ContentItem.Content.Step.Description.Text}}</p>
		</div>
	</div>
</li>
{% endif %}