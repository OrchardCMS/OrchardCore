{% zone "Header" %}
    <header class="masthead">
        <div class="container">
            <div class="masthead-subheading"></div>
            <div class="masthead-heading text-uppercase">{{ "Search" | t}}</div>
        </div>
    </header>
{% endzone %}

{{ Model.SearchForm | shape_render }}

{{ Model.SearchResults | shape_render }}

{{ Model.Pager | shape_render }}
