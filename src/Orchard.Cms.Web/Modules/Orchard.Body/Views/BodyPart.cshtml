@model dynamic
@inject Orchard.Tokens.Services.ITokenizer Tokenizer

@{ 
    string body = Model.Body;
    var context = Tokenizer.CreateViewModel().Content(Model.ContentItem);
    body = Tokenizer.Tokenize(body, context);
}

@Html.Raw(body)
