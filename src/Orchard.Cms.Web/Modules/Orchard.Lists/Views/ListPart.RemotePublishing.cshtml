@using Orchard.ContentManagement
@using Orchard.ResourceManagement
@inject IResourceManager ResourceManager

@{
    ContentItem contentItem = Model.ContentItem;
    ResourceManager.RegisterLink(new LinkEntry { Rel = "wlwmanifest", Type = "application/wlwmanifest+xml", Href = Url.Action("Manifest", "MetaWeblog", new { area = "Orchard.XmlRpc" }, Context.Request.Scheme) });
    ResourceManager.RegisterLink(new LinkEntry { Rel = "EditURI", Type = "application/rsd+xml", Title = "RSD", Href = Url.Action("Rsd", "RemotePublishing", new { area = "Orchard.Lists", contentItemId = contentItem.ContentItemId }, Context.Request.Scheme) });
}