@*TODO: Replace by display name*@
<h1>@RenderTitleSegments(T["Edit {0}", (string)Model.ContentItem.ContentType])</h1>

<form asp-action="Edit" method="post">
    @Html.ValidationSummary()
    @Display(Model)

    <button type="submit" class="btn btn-primary">Submit</button>
</form>
