@using Orchard.ContentManagement.Metadata.Models;

@{
    ContentPartFieldDefinition contentPartFieldDefinition = Model.ContentField;
}


@if (Model.Content != null)
{
    <fieldset class="form-group">
        @Display(Model.Content)
    </fieldset>
}

